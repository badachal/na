function loginValidation(){
    var email = document.getElementsById("email").value
    var password = document.getElementById("password").value
    
    
    // verifiy if the database has the corresponding email and password combination 
    if (email && password == )
        
        
    // if the password and email match return the corresponding role for that user
        
        // depening on the role load the approiate page based on user role 
        
        // else return error no match found
    
}